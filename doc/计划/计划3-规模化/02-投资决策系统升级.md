# 投资决策辅助系统升级计划

> **目标**: 从财报数据展示升级为投资决策辅助平台  
> **时间**: Month 3-4（60天）  
> **前置**: 计划3完成，30家公司数据完整  
> **预期ROI**: 提升用户停留时间300%+，增加付费转化潜力
> **更新**: 2026-02-11 - 新增券商研报集成方案、机构级基本面分析框架

---

## 🎯 战略升级

### 当前定位
**财报数据展示平台** → 查看单季度财报摘要

### 目标定位
**AI投资决策辅助系统** → 辅助用户做出买入/持有/卖出决策

### 核心价值主张
> "不只是告诉你发生了什么，而是告诉你应该怎么做"

### 差异化定位 (关键)

```
不要成为:
❌ 第二个Wind/同花顺 (追求数据最全)
❌ 第二个雪球 (追求社区活跃)
❌ 第二个Seeking Alpha (英文内容平台)

要成为:
✅ AI投资决策辅助 (中文 + 科技 + 专业)
✅ 散户友好的机构级分析 (化繁为简)
✅ 实时性 + 深度结合 (AI快速摘要)
✅ "量化+基本面"融合 (Quantamental)
```

---

## 📊 投资决策核心维度

### 维度1: 基本面分析 (Fundamental Analysis)

**当前状态**: ⚠️ 仅展示营收、EPS、净利润

**优化目标**:
- 盈利能力指标: ROE、ROA、毛利率、净利率
- 成长性指标: 营收增长率、EPS增长率、净利润增长率
- 财务健康: 负债率、现金流、自由现金流
- 效率指标: 资产周转率、存货周转率

**实施优先级**: ⭐⭐⭐⭐⭐

### 维度2: 估值分析 (Valuation Analysis)

**当前状态**: ❌ 无估值数据

**优化目标**:
- 绝对估值: P/E、P/B、P/S、EV/EBITDA
- 相对估值: 与行业平均对比、与历史区间对比
- 估值百分位: 当前估值处于5年区间的什么位置
- 合理估值区间: AI估算的目标价

**实施优先级**: ⭐⭐⭐⭐⭐

### 维度3: 行业对比 (Peer Comparison)

**当前状态**: ❌ 无同行对比

**优化目标**:
- 同行业公司关键指标对比表
- 行业排名百分位 (P/E排名第几、ROE排名第几)
- 竞争优势评估 (市场份额、增长率 vs 行业)

**实施优先级**: ⭐⭐⭐⭐

### 维度4: 宏观与政策 (Macro & Policy)

**当前状态**: ❌ 无宏观分析

**优化目标**:
- 利率环境对公司的影响评估
- 监管风险识别 (反垄断、数据隐私)
- 地缘政治因素分析
- 行业政策变化追踪

**实施优先级**: ⭐⭐⭐

### 维度5: AI投资建议 (Investment Recommendation)

**当前状态**: ⚠️ 仅情绪标签 (积极/中性/消极)

**优化目标**:
- 明确投资评级: 强烈买入/买入/持有/卖出/强烈卖出
- 目标价区间: AI估算的合理价格范围
- 投资期限建议: 短期/中期/长期
- 关键催化剂: 可能推动股价的正面事件
- 风险因素: 需要关注的主要风险

**实施优先级**: ⭐⭐⭐⭐⭐

---

## 📑 券商研报集成方案

### 为什么需要研报数据？

**机构投资者的投研工作流**:
```
1. 量化筛选 → 2. 研报深度阅读 → 3. 交叉验证 → 4. 投资决策
     │              │                │            │
     ▼              ▼                ▼            ▼
 财务数据      券商分析师观点      一致性检查    最终决策
 (已有)        (待集成)          (AI辅助)      (输出)
```

**研报数据价值**:
- 专业分析师的盈利预测 (EPS Forecast)
- 目标价共识 (Consensus Target Price)
- 行业趋势洞察 (Industry Insights)
- 管理层质量评估 (Management Assessment)

### 数据源选择策略

#### Tier 1: 专业数据终端 (推荐中长期)

| 数据源 | 覆盖内容 | 成本 | 接入方式 | 合规性 |
|--------|----------|------|----------|--------|
| **Wind (万得)** | 全市场研报、盈利预测 | ¥3-8万/年 | API/SDK | ⭐⭐⭐⭐⭐ |
| **同花顺iFinD** | 研报、产业链数据 | ¥1-3万/年 | API/Web | ⭐⭐⭐⭐ |
| **Choice** | 研报、基金数据 | ¥0.5-2万/年 | API | ⭐⭐⭐⭐ |

**推荐**: 初期使用 **Choice** (性价比高)，规模化后升级 **Wind**

#### Tier 2: 免费/低成本替代方案 (MVP阶段)

| 数据源 | 内容 | 技术方案 | 限制 |
|--------|------|----------|------|
| **FMP API** | 分析师评级、目标价 | REST API | 已集成，免费额度充足 |
| **SEC EDGAR** | 官方财报、管理层讨论 | 已集成 | 英文，格式固定 |
| **东方财富爬虫** | 研报摘要、评级 | 需开发 | 合规风险，需评估 |
| **财联社/华尔街见闻** | 新闻、快讯 | RSS/API | 时效性强 |

### 研报数据处理架构

```
┌──────────────────────────────────────────────────────────────┐
│                     研报数据Pipeline                          │
└──────────────────────────────────────────────────────────────┘

数据源层:
┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐
│ Wind API │ │ 券商FTP  │ │ 新闻RSS  │ │ FMP API  │
└────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘
     │            │            │            │
     └────────────┴────────────┴────────────┘
                  │
                  ▼
         ┌─────────────────┐
         │   ETL处理层      │
         │  ├─ PDF解析      │
         │  ├─ 文本提取     │
         │  ├─ 去重清洗     │
         │  └─ 质量检查     │
         └────────┬────────┘
                  │
                  ▼
         ┌─────────────────┐
         │   AI分析层       │
         │  ├─ 关键信息抽取 │
         │  ├─ 摘要生成     │
         │  ├─ 情绪分析     │
         │  └─ 一致性验证   │
         └────────┬────────┘
                  │
                  ▼
         ┌─────────────────┐
         │   存储层         │
         │  ├─ PostgreSQL   │
         │  ├─ pgvector     │
         │  └─ 向量检索     │
         └─────────────────┘
```

### 核心数据表设计

```sql
-- 研报主表
CREATE TABLE research_reports (
  id SERIAL PRIMARY KEY,
  symbol VARCHAR(10) NOT NULL,
  title TEXT NOT NULL,
  broker VARCHAR(50),              -- 券商名称
  analyst VARCHAR(50),             -- 分析师
  report_type VARCHAR(30),         -- 类型: industry/company/strategy
  rating VARCHAR(20),              -- 评级: buy/accumulate/neutral/reduce/sell
  target_price DECIMAL(10,2),      -- 目标价
  current_price DECIMAL(10,2),     -- 报告发布时股价
  upside DECIMAL(5,2),             -- 上涨空间 %
  publish_date DATE,
  summary TEXT,                    -- AI生成的摘要
  key_points JSONB,                -- 核心观点 ["point1", "point2"]
  risks JSONB,                     -- 风险提示
  forecasts JSONB,                 -- 盈利预测 {eps2024: x, eps2025: y}
  source_url TEXT,
  is_ai_generated BOOLEAN DEFAULT FALSE,
  created_at TIMESTAMP DEFAULT NOW()
);

-- 分析师追踪表
CREATE TABLE analyst_tracking (
  id SERIAL PRIMARY KEY,
  analyst_name VARCHAR(50),
  broker VARCHAR(50),
  sector VARCHAR(50),
  accuracy_score DECIMAL(4,2),     -- 历史准确率 0-100
  avg_return DECIMAL(5,2),         -- 推荐后平均收益
  total_recommendations INTEGER,
  last_updated TIMESTAMP
);

-- 盈利预测共识表
CREATE TABLE earnings_consensus (
  id SERIAL PRIMARY KEY,
  symbol VARCHAR(10),
  fiscal_year INTEGER,
  fiscal_quarter INTEGER,
  eps_consensus DECIMAL(10,2),     -- 分析师一致预期EPS
  eps_high DECIMAL(10,2),          -- 最高预期
  eps_low DECIMAL(10,2),           -- 最低预期
  revenue_consensus BIGINT,        -- 营收一致预期
  analyst_count INTEGER,           -- 覆盖分析师数量
  updated_at TIMESTAMP
);
```

### AI研报分析Prompt设计

```typescript
const RESEARCH_REPORT_PROMPT = `你是一位资深行业研究员，正在分析一份券商研究报告。
请从以下维度提取关键信息并生成结构化输出：

## 提取字段

### 1. 基本信息
- 报告类型: [行业研报/公司深度/策略报告/晨会纪要]
- 发布日期: YYYY-MM-DD
- 分析师: [姓名]
- 券商名称: [中金/中信/海通等]

### 2. 投资观点
- 投资评级: [买入/增持/中性/减持/卖出]
- 目标价: [价格] [币种]
- 当前价: [价格]
- 上涨空间: [%]

### 3. 核心逻辑 (3-5条)
- 主要论据和催化剂
- 每条用1句话概括

### 4. 关键数据
- 盈利预测 (当年/次年 EPS)
- 估值指标 (P/E, P/B, EV/EBITDA)
- 与市场预期差异

### 5. 风险提示
- 主要风险因素 (2-3条)

## 输出格式 (JSON)
{
  "reportType": string,
  "publishDate": string,
  "analyst": string,
  "broker": string,
  "rating": "buy" | "accumulate" | "neutral" | "reduce" | "sell",
  "targetPrice": number,
  "currentPrice": number,
  "upside": number,
  "keyPoints": string[],
  "forecasts": {
    "epsCurrentYear": number,
    "epsNextYear": number,
    "pe": number
  },
  "risks": string[],
  "sentiment": "positive" | "neutral" | "negative",
  "confidence": "high" | "medium" | "low"
}`;
```

### 合规与免责声明

```typescript
// 研报展示组件必须包含的免责声明
const RESEARCH_DISCLAIMER = `
⚠️ 免责声明：
1. 本页面引用研报观点仅供学习交流，不代表本站立场
2. 研报版权归原券商所有，请勿用于商业用途
3. 文中目标价、评级等具有时效性，请以最新报告为准
4. 分析师历史表现不代表未来准确性
5. 本网站不对因使用研报信息导致的损失负责

投资建议声明：
本网站提供的所有信息和分析仅供参考，不构成投资建议。
投资者应根据自身情况独立判断，谨慎决策。
`;
```

---

## 📄 原文展示系统设计

### 核心原则

**用户价值主张**: 
> "AI摘要帮助快速理解，原文提供深度验证"

**设计目标**:
1. **可信性** - 用户可查看原始数据验证AI分析
2. **深度** - 专业用户可直接阅读原文获取细节
3. **便利** - 一键切换AI摘要 vs 原文
4. **版权合规** - 合理引用、标注来源、不传播完整PDF

### 财报原文展示

#### 展示内容

**10-K/10-Q 年报/季报原文**:
```
┌─────────────────────────────────────────────────────────────────┐
│ 📄 AAPL FY2024 Q4 财报原文 (10-K)                               │
├─────────────────────────────────────────────────────────────────┤
│ 来源: SEC EDGAR | 发布日期: 2024-11-01 | 文件编号: 0000320193-24-000123│
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ 【AI摘要】              【原文】                                 │
│  ┌──────────┐          ┌──────────┐                            │
│  │  选中    │          │  选中    │                            │
│  └──────────┘          └──────────┘                            │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  📊 核心数据 (原文摘录)                                          │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ NET SALES                                               │   │
│  │   iPhone                        $43,810  $42,626  +2.8% │   │
│  │   Mac                            $7,741   $7,614  +1.7% │   │
│  │   iPad                           $6,995   $6,404  +9.2% │   │
│  │   Wearables...                   $9,042   $8,796  +2.8% │   │
│  │                                                         │   │
│  │ Net Income: $14,736 Million                             │   │
│  │ EPS (Diluted): $0.97                                    │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  📝 管理层讨论 (MD&A) - 原文                                    │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ ...the Company experienced strong demand for iPhone 16 │   │
│  │ driven by new AI features...                            │   │
│  │                                                         │   │
│  │ [查看完整MD&A段落]                                       │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ⚠️ 风险提示 (原文摘录)                                         │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ • The Company's operations...                           │   │
│  │ • Global economic conditions...                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│ 🔗 查看SEC官方原文 | 📥 下载PDF (外部链接)                        │
└─────────────────────────────────────────────────────────────────┘
```

#### 技术实现

```typescript
// 财报原文数据结构
interface EarningsDocument {
  id: number;
  symbol: string;
  fiscal_year: number;
  fiscal_quarter: number;
  report_date: string;
  
  // 元数据
  source: 'sec_edgar' | 'fmp' | 'company_website';
  document_type: '10-K' | '10-Q' | '8-K' | 'earnings_release';
  sec_filing_url?: string;      // SEC官方链接
  pdf_url?: string;             // PDF下载链接 (外部)
  
  // 原文内容 (结构化存储)
  content: {
    executive_summary?: string; // 执行摘要
    financial_highlights: {     // 财务亮点
      revenue: string;
      net_income: string;
      eps: string;
      yoy_changes: Record<string, string>;
    };
    md_and_a: string;           // 管理层讨论与分析
    risk_factors?: string[];    // 风险因素
    segment_performance?: Record<string, string>; // 分部门表现
    guidance?: string;          // 业绩指引
    balance_sheet_highlights?: string;
    cash_flow_highlights?: string;
  };
  
  // AI分析 (已有)
  ai_analysis: {
    summary: string;
    highlights: string[];
    concerns: string[];
    sentiment: string;
  };
  
  // 版权信息
  copyright_notice: string;
  last_updated: string;
}

// API端点
GET /api/earnings/[id]/document      // 获取财报原文
GET /api/earnings/[id]/summary       // 获取AI摘要 (已有)
GET /api/earnings/[id]/compare       // 对比原文vs AI摘要
```

#### 数据来源与获取

**SEC EDGAR** (主要来源):
- API: `https://www.sec.gov/Archives/edgar/daily-index/...`
- 获取最新10-K/10-Q文件
- 解析XBRL格式提取结构化数据
- 保留原始HTML/PDF链接

**FMP API** (补充):
- 已集成，提供标准化财务数据
- 可作为原文展示的补充数据源

**公司官网** (备选):
- 投资者关系页面 (IR)
- 财报新闻稿 (Press Release)
- Earnings Call Transcript

#### 内容处理流程

```
SEC EDGAR 文件
     │
     ▼
┌─────────────────┐
│ XBRL解析        │ ← 提取结构化财务数据
│ HTML提取        │ ← 提取管理层讨论原文
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ 内容分段        │
│ • 财务数据表格   │
│ • MD&A段落      │
│ • 风险因素      │
│ • 业绩指引      │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ AI对比分析      │ ← 验证AI摘要 vs 原文一致性
│ 关键信息标注    │ ← 高亮AI提到的关键数据
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ 数据库存储      │ ← PostgreSQL JSONB
└─────────────────┘
```

### 研报原文展示

#### 展示内容

**券商研报原文** (版权合规前提下):
```
┌─────────────────────────────────────────────────────────────────┐
│ 📑 中金公司 - AAPL深度研报 (2024-11-15)                         │
├─────────────────────────────────────────────────────────────────┤
│ 分析师: 张明 | 评级: 买入 ⭐ | 目标价: $220 (上涨空间 +12%)      │
│ 来源: 中金公司研究部 | 报告类型: 公司深度                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ 【AI摘要】              【研报原文】              【核心数据】   │
│  ┌──────────┐          ┌──────────┐          ┌──────────┐     │
│  │  选中    │          │  选中    │          │  选中    │     │
│  └──────────┘          └──────────┘          └──────────┘     │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  📝 投资逻辑 (AI摘要)                                            │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 1. iPhone 16 AI功能驱动换机周期，2025年出货量预计增长8%  │   │
│  │ 2. 服务业务毛利率提升至70%，成为利润增长主要驱动力       │   │
│  │ 3. 中国市场虽短期承压，但高端市场份额稳固               │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  📄 研报原文摘录 (核心观点)                                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 【原文】                                                 │   │
│  │ "我们认为iPhone 16系列搭载的Apple Intelligence将成为    │   │
│  │  推动换机的重要因素。根据我们的供应链调研，iPhone 16    │   │
│  │  首周末销量同比增长12%，超市场预期..."                    │   │
│  │                                                          │   │
│  │ 【关键数据】                                             │   │
│  │ • 2025年iPhone出货量预测: 2.35亿部 (+8% YoY)            │   │
│  │ • 服务业务收入预测: $95B (+15% YoY)                     │   │
│  │ • 目标市值: $3.4T                                       │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ⚠️ 风险提示 (原文)                                              │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 1. 宏观经济下行影响消费电子需求                          │   │
│  │ 2. 中国市场竞争加剧，华为、小米份额提升                  │   │
│  │ 3. AI功能落地进度不及预期                                │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  📊 盈利预测对比                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 指标        │ 中金预测 │ 市场一致预期 │ 差异           │   │
│  ├─────────────┼──────────┼──────────────┼────────────────┤   │
│  │ 2025 EPS    │ $7.20    │ $6.85        │ +5.1%         │   │
│  │ 2025 营收   │ $415B    │ $398B        │ +4.3%         │   │
│  │ 目标价      │ $220     │ $210         │ +4.8%         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│ ⚠️ 版权说明: 本页面仅展示研报核心观点摘录，完整报告请通过正规渠道获取  │
│ 🔗 查看中金官网 | 📧 联系中金销售 | 📥 下载完整PDF (需授权)         │
└─────────────────────────────────────────────────────────────────┘
```

#### 技术实现

```typescript
// 研报原文数据结构
interface ResearchReportDocument {
  id: number;
  symbol: string;
  
  // 基本信息
  title: string;
  broker: string;               // 券商名称
  analyst: string;              // 分析师
  co_analysts?: string[];       // 联席分析师
  report_date: string;
  report_type: 'industry' | 'company' | 'strategy' | 'macro';
  
  // 投资观点
  rating: 'buy' | 'accumulate' | 'neutral' | 'reduce' | 'sell';
  rating_changes?: string;      // 评级变动说明
  target_price: number;
  target_price_range?: {        // 目标价区间
    low: number;
    high: number;
  };
  current_price_at_publish: number;
  upside: number;
  
  // 原文内容 (结构化)
  content: {
    investment_thesis: string;  // 投资逻辑 (原文)
    key_points: string[];       // 核心观点列表
    financial_forecasts: {      // 盈利预测
      year: number;
      revenue: number;
      net_income: number;
      eps: number;
      pe_ratio: number;
    }[];
    valuation_analysis: string; // 估值分析原文
    risk_factors: string[];     // 风险提示
    catalysts: string[];        // 催化剂
  };
  
  // AI分析
  ai_summary: {
    key_takeaways: string[];
    disagreement_with_consensus?: string; // 与市场预期差异
    confidence_level: 'high' | 'medium' | 'low';
  };
  
  // 版权与来源
  source_url?: string;          // 券商官网链接
  pdf_download_url?: string;    // PDF下载 (需授权)
  copyright_holder: string;
  usage_terms: string;
  
  // 分析师追踪
  analyst_id?: number;          // 关联分析师历史准确率
}

// API端点
GET /api/research/[id]/document      // 获取研报原文
GET /api/research/[id]/summary       // 获取AI摘要
GET /api/research/[symbol]/latest    // 获取最新研报
GET /api/research/[symbol]/consensus // 获取分析师一致预期
```

#### 版权合规策略

**展示原则**:
```typescript
const COPYRIGHT_POLICY = {
  // ✅ 可以展示
  allowed: [
    '研报标题、作者、日期',
    '投资评级、目标价 (事实性信息)',
    '核心观点摘要 (不超过原文30%)',
    '关键数据、预测表格',
    '风险提示列表',
    '券商官方链接'
  ],
  
  // ❌ 不可展示
  prohibited: [
    '完整PDF文件 (无授权)',
    '大段原文 (>500字连续摘录)',
    '图表、图片 (需单独授权)',
    '详细财务模型',
    '敏感性分析表格'
  ],
  
  // ⚠️ 需要处理
  requirements: [
    '显著标注来源和版权归属',
    '提供券商官方链接',
    '声明"仅供参考，不构成投资建议"',
    '用户需登录后查看 (访问控制)',
    '禁止复制/下载功能 (或加水印)'
  ]
};
```

**数据获取方式**:

| 方式 | 成本 | 合规性 | 适用阶段 |
|------|------|--------|----------|
| **Wind/Choice API** | ¥500-8000/月 | ⭐⭐⭐⭐⭐ | 规模化后 |
| **券商官方合作** | 免费-象征性 | ⭐⭐⭐⭐⭐ | 中长期 |
| **公开摘要聚合** | 免费 | ⭐⭐⭐ | MVP阶段 |
| **用户上传** | 免费 | ⭐⭐ | 社区化 |

### 原文展示UI组件设计

#### 1. 文档查看器组件

```typescript
// DocumentViewer.tsx
interface DocumentViewerProps {
  type: 'earnings' | 'research';
  documentId: string;
  defaultView: 'ai-summary' | 'original' | 'split';
}

const DocumentViewer: React.FC<DocumentViewerProps> = ({
  type,
  documentId,
  defaultView = 'split'
}) => {
  const [viewMode, setViewMode] = useState(defaultView);
  const [activeSection, setActiveSection] = useState('all');
  
  return (
    <div className="document-viewer">
      {/* 视图切换 */}
      <ViewModeSelector 
        current={viewMode}
        onChange={setViewMode}
        options={['ai-summary', 'original', 'split', 'compare']}
      />
      
      {/* 内容区域 */}
      <div className="content-area">
        {viewMode === 'split' && (
          <SplitView>
            <AISummaryPanel documentId={documentId} />
            <OriginalDocumentPanel documentId={documentId} />
          </SplitView>
        )}
        
        {viewMode === 'compare' && (
          <CompareView documentId={documentId} />
        )}
        
        {viewMode === 'ai-summary' && (
          <AISummaryPanel documentId={documentId} fullWidth />
        )}
        
        {viewMode === 'original' && (
          <OriginalDocumentPanel documentId={documentId} fullWidth />
        )}
      </div>
      
      {/* 章节导航 */}
      <SectionNavigator 
        sections={['财务数据', '管理层讨论', '风险因素', '业绩指引']}
        active={activeSection}
        onSelect={setActiveSection}
      />
      
      {/* 来源标注 */}
      <SourceAttribution type={type} documentId={documentId} />
    </div>
  );
};
```

#### 2. AI摘要 vs 原文对比视图

```typescript
// CompareView.tsx - 高亮AI摘要与原文的对应关系
const CompareView = ({ documentId }) => {
  return (
    <div className="compare-view">
      {/* AI摘要 - 可点击跳转到原文 */}
      <div className="ai-summary-panel">
        <h3>AI摘要</h3>
        {aiSummary.highlights.map((point, idx) => (
          <div 
            key={idx}
            className="highlight-point"
            data-source-section={point.sourceSection}
            onClick={() => scrollToOriginal(point.sourceSection)}
          >
            <span className="marker">{idx + 1}</span>
            <p>{point.text}</p>
            <span className="source-link">查看原文 →</span>
          </div>
        ))}
      </div>
      
      {/* 原文 - 高亮AI引用的部分 */}
      <div className="original-panel">
        <h3>原文</h3>
        {originalContent.sections.map((section) => (
          <div 
            key={section.id}
            className={`section ${section.isCited ? 'cited' : ''}`}
            data-section-id={section.id}
          >
            {section.isCited && (
              <span className="citation-marker">
                被AI摘要引用
              </span>
            )}
            <h4>{section.title}</h4>
            <p>{section.content}</p>
          </div>
        ))}
      </div>
    </div>
  );
};
```

#### 3. 关键数据提取展示

```typescript
// KeyMetricsExtractor.tsx
const KeyMetricsExtractor = ({ documentId }) => {
  const metrics = useKeyMetrics(documentId);
  
  return (
    <div className="key-metrics-panel">
      <h3>📊 关键数据提取</h3>
      
      {/* AI提取的数据 */}
      <div className="ai-extracted">
        <h4>AI识别 (自动提取)</h4>
        {metrics.aiExtracted.map((metric) => (
          <div key={metric.id} className="metric-item">
            <span className="label">{metric.label}</span>
            <span className="value">{metric.value}</span>
            <span className="source">来源: {metric.source}</span>
          </div>
        ))}
      </div>
      
      {/* 与原文对比 */}
      <div className="verification">
        <h4>✓ 原文验证</h4>
        <VerificationBadge 
          status={metrics.verificationStatus}
          details={metrics.verificationDetails}
        />
      </div>
      
      {/* 用户纠错 */}
      <div className="user-correction">
        <h4>发现错误?</h4>
        <CorrectionButton documentId={documentId} />
      </div>
    </div>
  );
};
```

### 数据存储方案

#### 数据库设计

```sql
-- 财报原文表
CREATE TABLE earnings_documents (
  id SERIAL PRIMARY KEY,
  earnings_id INTEGER REFERENCES earnings(id),
  
  -- 来源信息
  source VARCHAR(50),              -- sec_edgar, fmp, company_ir
  source_url TEXT,
  document_type VARCHAR(20),       -- 10-K, 10-Q, 8-K, earnings_release
  filing_date DATE,
  
  -- 原文内容 (JSONB存储结构化内容)
  content JSONB,
  
  -- 原始文件存储 (外部存储)
  raw_html_url TEXT,               -- 原始HTML链接
  raw_pdf_url TEXT,                -- PDF链接 (外部)
  
  -- AI分析关联
  ai_analysis_id INTEGER REFERENCES ai_analyses(id),
  
  -- 元数据
  page_count INTEGER,
  word_count INTEGER,
  language VARCHAR(10),
  
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP
);

-- 研报原文表
CREATE TABLE research_documents (
  id SERIAL PRIMARY KEY,
  report_id INTEGER REFERENCES research_reports(id),
  
  -- 原文内容 (结构化)
  content JSONB,
  
  -- 原始文件 (需授权)
  pdf_url TEXT,
  pdf_size_mb DECIMAL(5,2),
  
  -- 访问控制
  access_level VARCHAR(20),        -- public, registered, premium
  download_allowed BOOLEAN DEFAULT FALSE,
  
  -- 版权信息
  copyright_holder VARCHAR(100),
  license_type VARCHAR(50),
  
  created_at TIMESTAMP DEFAULT NOW()
);

-- 原文访问日志 (版权合规审计)
CREATE TABLE document_access_logs (
  id SERIAL PRIMARY KEY,
  document_id INTEGER,
  document_type VARCHAR(20),       -- earnings, research
  user_id INTEGER,                 -- 匿名用户为NULL
  ip_address INET,
  access_type VARCHAR(20),         -- view, download, share
  accessed_at TIMESTAMP DEFAULT NOW()
);
```

#### 文件存储策略

```typescript
// 存储策略
const STORAGE_STRATEGY = {
  // 财报原文 (SEC EDGAR - 公开数据)
  earnings: {
    storage: 'external_link',      // 只存链接，不存文件
    cache: '7_days',               // 缓存7天
    backup: false                  // SEC官网永久保存
  },
  
  // 研报原文 (版权受限)
  research: {
    storage: 'no_storage',         // 不存储完整PDF
    excerpt_only: true,            // 只存摘要摘录 (<30%)
    cache: '24_hours',
    access_control: 'authenticated' // 需登录
  },
  
  // AI处理后的结构化数据
  structured: {
    storage: 'postgresql_jsonb',   // 数据库存储
    retention: 'permanent',
    backup: true
  }
};
```

### 性能优化

```typescript
// 原文加载优化策略
const PERFORMANCE_OPTIMIZATION = {
  // 1. 按需加载 (Lazy Loading)
  lazyLoading: {
    initial: 'ai_summary_only',    // 默认只加载AI摘要
    threshold: 'viewport',         // 进入视口才加载原文
    chunkSize: '1000_chars'        // 分块加载大文档
  },
  
  // 2. 缓存策略
  caching: {
    aiSummary: '24_hours',         // AI摘要缓存24小时
    originalExcerpt: '7_days',     // 原文摘录缓存7天
    staticAssets: '30_days'        // 静态资源缓存30天
  },
  
  // 3. CDN加速
  cdn: {
    provider: 'Vercel Edge',
    regions: ['global'],
    compression: 'gzip'
  },
  
  // 4. 预加载
  prefetching: {
    onHover: 'document_metadata',  // 悬停预加载元数据
    onScroll: 'next_section'       // 滚动预加载下一章节
  }
};
```

---

## 📊 机构级基本面分析框架

### 分析理念: Quantamental (量化+基本面融合)

```
现代机构投资分析框架:

定量筛选 (Quant)          基本面深度 (Fundamental)
      │                           │
      ├─ 财务指标过滤              ├─ 商业模式分析
      ├─ 估值模型打分              ├─ 护城河评估
      ├─ 技术信号                  ├─ 管理层质量
      └─ 风险模型                  └─ 行业趋势
            │                           │
            └───────────┬───────────────┘
                        │
                        ▼
                ┌───────────────┐
                │  综合决策引擎  │
                │  (AI辅助)     │
                └───────────────┘
```

### 五维分析框架

#### 维度1: 财务质量分析 (Financial Quality)

**1.1 盈利能力评分卡**

```
┌──────────────────────────────────────────────────────────────┐
│                  盈利能力评分卡 (满分100分)                    │
├──────────────────────────────────────────────────────────────┤
│ 指标                  │ 权重  │ 优秀标准     │ 计算方式      │
├───────────────────────┼───────┼──────────────┼───────────────┤
│ ROE (净资产收益率)     │ 20%   │ >15%         │ 净利润/净资产 │
│ ROIC (投入资本回报率)  │ 15%   │ >12%         │ NOPAT/投入资本│
│ 毛利率                │ 15%   │ >40%         │ 毛利/营收     │
│ 营业利润率             │ 15%   │ >20%         │ 营业利润/营收 │
│ 净利润率              │ 15%   │ >15%         │ 净利润/营收   │
│ EPS增长率 (3年CAGR)   │ 20%   │ >20%         │ 复合增长率    │
└──────────────────────────────────────────────────────────────┘
```

**1.2 杜邦分析模型**

```
ROE = 净利润率 × 资产周转率 × 权益乘数

        净利润          营收            总资产
      = ─────── × ─────────── × ───────────
         营收           总资产          净资产

          │              │               │
          ▼              ▼               ▼
       【盈利能力】   【运营效率】     【财务杠杆】
```

**1.3 现金流健康度检查**

```typescript
interface CashFlowHealth {
  // 经营现金流质量
  ocfToNetIncome: number;      // OCF/净利润 > 1 健康
  fcfToRevenue: number;        // FCF/营收 > 10% 优秀
  
  // 资本支出效率
  capexToOcf: number;          // 资本支出/OCF < 50% 健康
  
  // 股东回报能力
  dividendPayoutRatio: number; // 分红率 30-50% 合理
  
  // 预警指标
  redFlags: {
    decliningOcf: boolean;     // OCF连续下滑
    negativeFcf: boolean;      // FCF为负(扩张期除外)
    risingReceivables: boolean; // 应收账款增速>营收增速
  };
}
```

#### 维度2: 成长性分析 (Growth Analysis)

**2.1 增长质量评估**

```
成长类型识别:

🚀 高质量增长          ⚠️ 低质量增长
─────────────────────────────────────────
营收增长 + 利润增长    营收增长 + 利润下滑
(规模效应体现)         (价格战牺牲利润)

内生增长               并购增长
(有机增长，可持续)      (整合风险)

市场份额提升           行业整体扩张
(竞争力强)             (随波逐流)

新产品驱动             单一产品依赖
(创新驱动)             (风险集中)
```

**2.2 S曲线生命周期定位**

```
        营收
         │      ╭────────── 成熟期
         │     ╱             (现金流牛)
         │    ╱   
         │   ╱    ╭──────── 成长期
         │  ╱    ╱           (高增长)
         │ ╱    ╱
         │╱    ╱  ╭──────── 导入期
         ├────┴──╱           (高风险)
         │
         └───────────────────► 时间
```

#### 维度3: 竞争护城河分析 (Competitive Moat)

**3.1 护城河类型评估**

```
护城河强度评分 (0-10分)

🟢 宽护城河 (8-10分)
├─ 网络效应: Meta, Visa
├─ 品牌壁垒: Apple, Nike
├─ 成本优势: Walmart, BYD
├─ 技术壁垒: NVDA (CUDA), ASML
└─ 监管壁垒: 金融牌照

🟡 窄护城河 (4-7分)
├─ 先发优势: Netflix, Tesla
├─ 客户粘性: Salesforce
├─ 专利保护: 制药公司
└─ 渠道控制: Costco, 茅台

🔴 无护城河 (0-3分)
├─ 商品化业务: 传统零售
└─ 低门槛行业: 餐饮、基础制造
```

**3.2 波特五力量化模型**

```typescript
interface PorterFiveForces {
  industryRivalry: {
    score: number; // 0-10
    factors: {
      marketGrowth: 'low' | 'medium' | 'high';
      concentration: 'fragmented' | 'consolidated';
      productDifferentiation: number;
      exitBarriers: 'low' | 'high';
    };
  };
  
  threatOfEntry: {
    score: number;
    factors: {
      capitalRequirements: 'low' | 'medium' | 'high';
      economiesOfScale: boolean;
      brandLoyalty: 'low' | 'high';
      regulatoryBarriers: string[];
    };
  };
  
  threatOfSubstitution: {
    score: number;
    factors: {
      substituteAvailability: string[];
      switchingCosts: 'low' | 'high';
      pricePerformance: string;
    };
  };
  
  supplierPower: {
    score: number;
    factors: {
      supplierConcentration: number;
      switchingCosts: 'low' | 'high';
      forwardIntegration: boolean;
    };
  };
  
  buyerPower: {
    score: number;
    factors: {
      buyerConcentration: number;
      priceSensitivity: 'low' | 'high';
      backwardIntegration: boolean;
    };
  };
  
  overallAttractiveness: number; // 0-100分
}
```

#### 维度4: 估值分析 (Valuation)

**4.1 多维度估值框架**

```
估值方法选择决策树:

公司类型                    推荐估值方法
─────────────────────────────────────────────
盈利稳定 + 成熟企业    →   P/E + P/B + DCF
高成长 + 未盈利        →   P/S + EV/Revenue + 可比交易
资产密集型             →   P/B + NAV + 重置成本
平台型/网络型          →   P/MAU + EV/Gross Profit
周期型企业             →   周期调整P/E + NAV
```

**4.2 DCF模型关键假设**

```typescript
interface DCFModel {
  // 收入预测 (5年)
  revenueForecast: {
    year1: number;
    year2: number;
    year3: number;
    year4: number;
    year5: number;
    longTermGrowth: number; // 永续增长率 2-3%
  };
  
  // 利润率假设
  marginAssumptions: {
    grossMargin: number;
    operatingMargin: number;
    taxRate: number;
  };
  
  // 折现率计算 (WACC)
  wacc: {
    riskFreeRate: number;   // 无风险利率
    beta: number;
    marketRiskPremium: number; // 5-7%
    costOfEquity: number;   // Rf + β × MRP
    costOfDebt: number;
    debtToCapital: number;
  };
  
  // 估值输出
  output: {
    enterpriseValue: number;
    equityValue: number;
    fairValuePerShare: number;
    upside: number;
    sensitivity: {
      bullCase: number;
      baseCase: number;
      bearCase: number;
    };
  };
}
```

#### 维度5: 风险与催化剂 (Risks & Catalysts)

**5.1 风险矩阵评估**

```typescript
interface RiskMatrix {
  // 市场风险
  marketRisks: {
    beta: number;
    correlationWithMarket: number;
    drawdownHistory: number;
  };
  
  // 业务风险
  businessRisks: {
    customerConcentration: number; // >20%危险
    productConcentration: number;
    geographicExposure: string[];
    regulatoryRisk: 'low' | 'medium' | 'high';
  };
  
  // 财务风险
  financialRisks: {
    debtToEquity: number;
    interestCoverage: number;
    currentRatio: number;
    altmanZScore: number; // 破产风险评分
  };
  
  // ESG风险
  esgRisks: {
    environmental: number;
    social: number;
    governance: number;
  };
  
  // 催化剂日历
  catalysts: {
    date: string;
    event: string;
    impact: 'high' | 'medium' | 'low';
    probability: number;
  }[];
}
```

---

## 🏗️ 技术架构升级

### 数据层扩展

#### 新增数据表

```sql
-- 公司估值指标表
CREATE TABLE company_valuation (
  id SERIAL PRIMARY KEY,
  company_id INTEGER REFERENCES companies(id),
  market_cap BIGINT,
  pe_ratio DECIMAL(10,2),
  pb_ratio DECIMAL(10,2),
  ps_ratio DECIMAL(10,2),
  ev_ebitda DECIMAL(10,2),
  roe DECIMAL(5,2),
  roa DECIMAL(5,2),
  debt_to_equity DECIMAL(5,2),
  free_cash_flow BIGINT,
  updated_at TIMESTAMP
);

-- 行业基准表
CREATE TABLE industry_benchmarks (
  id SERIAL PRIMARY KEY,
  sector VARCHAR(50),
  metric_name VARCHAR(50),
  avg_value DECIMAL(10,2),
  median_value DECIMAL(10,2),
  updated_at TIMESTAMP
);

-- 竞争对比表
CREATE TABLE peer_comparison (
  id SERIAL PRIMARY KEY,
  company_id INTEGER REFERENCES companies(id),
  peer_symbol VARCHAR(10),
  metric VARCHAR(50),
  company_value DECIMAL(10,2),
  peer_value DECIMAL(10,2),
  percentile INTEGER,
  comparison_date DATE
);

-- 宏观政策表
CREATE TABLE macro_policies (
  id SERIAL PRIMARY KEY,
  policy_type VARCHAR(50),
  title TEXT,
  description TEXT,
  impact_sectors TEXT[],
  sentiment VARCHAR(20),
  effective_date DATE,
  created_at TIMESTAMP
);
```

#### 数据源扩展

| 数据源 | 用途 | 成本 |
|--------|------|------|
| Yahoo Finance API | 估值指标 (P/E, P/B等) | 免费 |
| Alpha Vantage | 技术指标、历史价格 | 免费 (5次/分钟) |
| FRED API | 宏观数据 (利率、通胀) | 免费 |
| NewsAPI | 政策新闻 | 免费 (100次/天) |

### AI分析引擎升级

#### 新Prompt架构

```
现有: 财报摘要生成
优化: 多维度投资决策分析

分析流程:
财报数据 ──┬──→ 基本面评估 ──┐
          ├──→ 估值分析 ────┼──→ 综合投资决策
          ├──→ 行业对比 ────┤
          └──→ 宏观影响 ────┘
                    ↓
         投资建议（评级+目标价+风险）
```

#### AI输出结构

```typescript
interface InvestmentAnalysis {
  investmentRating: 'strong_buy' | 'buy' | 'hold' | 'sell' | 'strong_sell';
  confidence: 'high' | 'medium' | 'low';
  targetPrice: { low: number; high: number };
  
  valuation: {
    assessment: 'undervalued' | 'fair' | 'overvalued';
    pePercentile: number; // 0-100
    reasoning: string;
  };
  
  fundamentals: {
    strengths: string[];
    weaknesses: string[];
    trend: 'improving' | 'stable' | 'deteriorating';
  };
  
  competitivePosition: {
    moat: 'wide' | 'narrow' | 'none';
    marketPosition: 'leader' | 'challenger' | 'follower';
    vsPeers: string[];
  };
  
  risks: string[];
  catalysts: string[];
  investmentHorizon: 'short' | 'medium' | 'long';
}
```

---

## 🎨 UI/UX重新设计

### 新增核心页面

```
现有页面:
├── /                    # 首页
├── /companies           # 公司列表
├── /earnings/[symbol]   # 财报详情
├── /calendar            # 财报日历
└── /about               # 关于

新增页面:
├── /dashboard                    # 投资决策仪表盘（新首页）⭐
├── /analysis/[symbol]            # 深度分析页 ⭐
│   ├── /valuation               # 估值分析
│   ├── /fundamentals            # 基本面分析
│   ├── /peers                   # 竞争对比
│   └── /forecast                # 业绩预测
├── /sectors                      # 行业分析
├── /macro                        # 宏观政策
└── /portfolio                    # 投资组合（登录用户）
```

### 关键组件设计

#### 1. 投资评级卡片

```
┌─────────────────────────────────────┐
│  🏆 投资评级                         │
├─────────────────────────────────────┤
│                                     │
│     ┌───────────────┐              │
│     │    买入 ⭐    │              │
│     │  置信度: 高   │              │
│     └───────────────┘              │
│                                     │
│  目标价区间: $180 - $220           │
│  当前价: $195 (合理估值)            │
│                                     │
│  建议持仓期限: 长期 (1-3年)         │
│                                     │
└─────────────────────────────────────┘
```

#### 2. 估值仪表盘

```
┌─────────────────────────────────────┐
│  📊 估值分析                         │
├─────────────────────────────────────┤
│                                     │
│   当前P/E: 28.5x                    │
│                                     │
│   历史区间: [████░░░░░░] 25%分位    │
│   行业平均: 32.1x (低于行业11%)     │
│                                     │
│   评估: 🟢 低估                     │
│   合理P/E区间: 25x - 35x           │
│                                     │
└─────────────────────────────────────┘
```

#### 3. 行业对比表格

```
┌──────────────────────────────────────────────────────┐
│  🏢 行业对比 (芯片行业)                              │
├──────────────────────────────────────────────────────┤
│  公司    │ P/E   │ ROE   │ 营收增长 │ 行业排名      │
├──────────┼───────┼───────┼──────────┼───────────────┤
│  NVDA ⭐ │ 35.2x │ 45%   │ +85%     │ #1 综合       │
│  AMD     │ 28.5x │ 12%   │ +15%     │ #2 综合       │
│  INTC    │ 18.2x │ 8%    │ -5%      │ #5 综合       │
├──────────┴───────┴───────┴──────────┴───────────────┤
│  本公司在行业中P/E排名第2，ROE排名第1               │
└──────────────────────────────────────────────────────┘
```

---

## 📅 实施路线图 (更新版)

### Phase 1: 数据基础设施 (Week 1-2)

**1.1 核心数据表搭建**
- [ ] 创建 `company_valuation` 估值指标表
- [ ] 创建 `industry_benchmarks` 行业基准表
- [ ] 创建 `peer_comparison` 竞争对比表
- [ ] 创建 `research_reports` 研报主表
- [ ] 创建 `analyst_tracking` 分析师追踪表
- [ ] 创建 `earnings_consensus` 盈利预测共识表

**1.2 数据源集成**
- [ ] 集成Yahoo Finance API (估值数据)
- [ ] 集成Alpha Vantage (技术指标)
- [ ] 评估Choice/Wind API (研报数据)
- [ ] 搭建PDF解析pipeline (备选方案)

**1.3 数据同步脚本**
- [ ] 估值数据日同步脚本
- [ ] 行业基准周更新脚本
- [ ] 研报数据获取脚本
- [ ] 数据质量检查脚本

### Phase 2: AI分析引擎升级 (Week 3-4)

**2.1 Prompt工程**
- [ ] 设计五维分析Prompt (财务/成长/护城河/估值/风险)
- [ ] 设计研报解析Prompt
- [ ] 设计投资评级生成Prompt
- [ ] A/B测试不同Prompt效果

**2.2 AI分析模块开发**
- [ ] 财务质量评分模块 (杜邦分析)
- [ ] 成长性评估模块 (S曲线定位)
- [ ] 护城河评估模块 (波特五力)
- [ ] 估值分析模块 (多方法综合)
- [ ] 风险识别模块 (多维度风险矩阵)

**2.3 研报数据处理**
- [ ] PDF文本提取功能
- [ ] 关键信息抽取 (评级/目标价/观点)
- [ ] 分析师历史准确率计算
- [ ] 研报摘要自动生成

### Phase 3: API与后端开发 (Week 5)

**3.1 核心API端点**
```typescript
// 估值数据
GET /api/valuation/[symbol]
// 行业对比
GET /api/peers/[symbol]
// 投资建议 (综合)
GET /api/analysis/[symbol]/investment
// 行业列表
GET /api/sectors
// 研报列表
GET /api/research/[symbol]
// 盈利预测共识
GET /api/consensus/[symbol]
```

**3.2 数据聚合服务**
- [ ] 多数据源聚合 (FMP + Yahoo + 研报)
- [ ] 缓存策略 (Redis)
- [ ] 数据一致性检查
- [ ] 异常数据预警

### Phase 4: UI/UX升级 (Week 6-7)

**4.1 新增页面**
- [ ] `/dashboard` - 投资决策仪表盘 (新首页)
- [ ] `/analysis/[symbol]` - 公司深度分析页
- [ ] `/valuation/[symbol]` - 估值分析详情
- [ ] `/peers/[symbol]` - 竞争对比页
- [ ] `/sectors/[sector]` - 行业分析页

**4.2 核心组件**
- [ ] 投资评级卡片 (买入/增持/中性/减持/卖出)
- [ ] 估值仪表盘 (历史区间 + 行业对比)
- [ ] 财务健康度评分卡 (ROE/ROIC/现金流)
- [ ] 护城河评估图 (五力模型可视化)
- [ ] 研报聚合展示 (多券商观点对比)
- [ ] 催化剂日历 (Upcoming Events)
- [ ] 风险预警卡片 (风险等级+应对措施)

**4.3 数据可视化**
- [ ] 杜邦分析拆解图
- [ ] S曲线生命周期定位
- [ ] 估值历史走势图
- [ ] 行业排名对比图
- [ ] 分析师预期分布图

### Phase 5: 智能化功能 (Week 8)

**5.1 AI投资助手**
- [ ] 自然语言问答 ("AAPL现在值得买吗?")
- [ ] 公司对比分析 ("NVDA vs AMD")
- [ ] 行业趋势解读 ("AI芯片行业前景")

**5.2 智能预警系统**
- [ ] 评级变化预警 (分析师上调/下调)
- [ ] 估值异常预警 (P/E突破历史区间)
- [ ] 风险事件预警 (监管、诉讼、业绩miss)
- [ ] 催化剂提醒 (财报发布、产品发布)

**5.3 投资组合分析 (V2)**
- [ ] 持仓风险评估
- [ ] 行业配置建议
- [ ] 相关性分析
- [ ] 再平衡建议

---

## 🚀 立即可做的快速优化 (按优先级)

### 本周内完成 (Day 1-3)

**1. 升级AI Prompt** ⭐⭐⭐⭐⭐
```typescript
// 替换现有的简单Prompt
const NEW_PROMPT = `作为CFA持证分析师，基于以下数据生成投资分析报告：

【输入数据】
1. 财务数据: [营收、EPS、净利润、现金流]
2. 估值数据: [P/E、P/B、PS、EV/EBITDA]
3. 行业对比: [同行数据]
4. 宏观环境: [利率、政策]

【分析框架】
1. 财务质量评分 (ROE杜邦分析、ROIC、现金流健康度)
2. 成长性评估 (营收增长、pipeline、S曲线定位)
3. 护城河识别 (竞争优势、波特五力)
4. 估值判断 (DCF、相对估值、历史分位)
5. 风险与催化剂 (3大风险 + 3大催化剂)

【输出要求】
- 明确投资评级: 强烈买入/买入/持有/卖出/强烈卖出
- 目标价区间: [低-高] + 置信度
- 核心逻辑: 3-5条bullet points
- 主要风险: 2-3条
- 关键催化剂: 列出 upcoming events

用JSON格式输出。`;
```

**2. 添加基础估值显示** ⭐⭐⭐⭐
- [ ] 集成Yahoo Finance API获取实时P/E、P/B
- [ ] 在财报页显示当前估值vs行业平均
- [ ] 标注估值偏高/偏低/合理

### 本周内完成 (Day 4-7)

**3. 投资评级标签** ⭐⭐⭐⭐
- [ ] 根据AI分析结果显示投资评级
- [ ] 添加置信度标签 (高/中/低)
- [ ] 显示目标价区间

**4. 同行对比表格** ⭐⭐⭐
- [ ] 同行业公司关键指标对比
- [ ] 显示行业排名 (P/E排名、ROE排名)
- [ ] 标注领先/落后指标

### 下周完成 (Week 2)

**5. 财务健康度仪表盘** ⭐⭐⭐
- [ ] ROE杜邦分析拆解
- [ ] 现金流健康度评分
- [ ] 成长性指标展示

**6. 研报摘要集成 (MVP)** ⭐⭐⭐
- [ ] 接入FMP分析师评级API
- [ ] 显示目标价共识
- [ ] 展示评级分布图

---

## ✅ 验收标准 (更新)

### 功能指标
- [ ] 支持30家公司的五维投资分析
- [ ] AI生成明确投资评级 (买入/持有/卖出)
- [ ] 显示目标价区间 + 置信度
- [ ] 估值仪表盘 (历史区间 + 行业对比)
- [ ] 同行对比功能 (行业排名)
- [ ] 基础研报数据集成 (分析师评级)
- [ ] AI投资建议准确率 > 65% (回测验证)

### 用户体验
- [ ] 用户停留时间 > 3分钟
- [ ] 页面深度 > 3页
- [ ] 回访率 > 40%
- [ ] 投资评级点击率 > 30%

### 商业指标
- [ ] 邮件订阅转化率 > 5%
- [ ] 用户主动分享率 > 2%
- [ ] 付费转化率 > 1% (V2阶段)

---

## 💰 成本估算 (更新)

| 项目 | 月成本 | 说明 |
|------|--------|------|
| DeepSeek API | ¥20-40 | 复杂分析prompt，token增加 |
| Yahoo Finance | ¥0 | 免费API |
| Alpha Vantage | ¥0 | 免费额度 |
| Choice数据 (可选) | ¥500-2000 | 研报数据 (初期可跳过) |
| Wind数据 (远期) | ¥3000-8000 | 规模化后升级 |
| **MVP总计** | **¥20-40** | 不含付费数据终端 |
| **完整版总计** | **¥500-1000** | 含Choice研报数据 |

---

## 📚 相关文档

- [技术架构设计](../技术/架构设计.md)
- [数据模型](../技术/数据模型.md)
- [运营策略](../策略/运营策略.md)
- [计划3-规模化](./00-计划.md)

---

**前置条件**: 计划3完成，30家公司数据覆盖
**负责人**: AI Agent + 开发者
**创建日期**: 2026-02-11
**最后更新**: 2026-02-11 (新增券商研报集成、机构级基本面分析框架)
**版本**: v2.0

---

## ✅ 验收标准

### 功能指标
- [ ] 支持30家公司的完整投资分析
- [ ] 每家公司显示投资评级 (买入/持有/卖出)
- [ ] 估值仪表盘展示P/E历史区间
- [ ] 行业对比功能上线
- [ ] AI投资建议置信度>70%

### 用户体验
- [ ] 用户停留时间 > 3分钟 (vs 当前1分钟)
- [ ] 页面深度 > 3页 (vs 当前1.5页)
- [ ] 回访率 > 40%

### 商业指标
- [ ] 邮件订阅转化率 > 5%
- [ ] 用户主动分享率 > 2%

---

## 💰 成本估算

| 项目 | 月成本 | 说明 |
|------|--------|------|
| DeepSeek API | ¥20-30 | 复杂分析prompt，token增加 |
| Yahoo Finance | ¥0 | 免费API |
| Alpha Vantage | ¥0 | 免费额度 |
| NewsAPI | ¥0 | 免费额度 |
| **总计** | **¥20-30** | |

---

## 🚀 立即可做的快速优化

### 第1天: 最快见效
1. **升级AI Prompt** - 添加投资建议维度
2. **添加估值指标显示** - 在财报页显示P/E

### 第1周: 短期提升
1. **投资评级标签** - 显示买入/持有/卖出
2. **行业对比表格** - 同行业公司横向对比

### 第2周: 中期升级
1. **Dashboard页面** - AI推荐 + 市场概览
2. **估值历史图表** - P/E区间可视化

---

## 📁 相关文档

- [技术架构设计](../技术/架构设计.md)
- [数据模型](../技术/数据模型.md)
- [运营策略](../策略/运营策略.md)
- [计划3-规模化](./00-计划.md)

---

**前置条件**: 计划3完成，30家公司数据覆盖
**负责人**: AI Agent + 开发者
**创建日期**: 2026-02-11
**最后更新**: 2026-02-11
