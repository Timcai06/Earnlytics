# 数据一致性与幂等（新手也要懂）

## 什么是幂等

同一个脚本重复执行多次，结果应该一致，不应无限重复插入脏数据。

## 项目里的幂等做法

1. `upsert` + `onConflict`
2. 例如按 `company_id,fiscal_year,fiscal_quarter` 去重

## 为什么这很重要

1. 定时任务会重复运行
2. 网络抖动会导致重试
3. 没有幂等就会出现重复数据

## 你写脚本时的自检清单

1. 有没有唯一键约束
2. 写入是否用 upsert
3. 重跑脚本是否会产生额外垃圾数据

